1.第一个字符大写
capitalize()
2.全部转换为小写
casefold()
3.居中后用空格填充至指定长度width
center(width)
4.计算sep出现次数
count(sep)
5.以encoding编码字符串
encode()
6.判断最后一个字符是否为指定字符sep
endswith(sep)
7.用空格替换制表符(\t)
expandtabs()
8.从左起第一个找到的sep，返回索引值，无则返回-1
find()
9.字符串格式化操作
format()
10.从左起第一个找到的sep，返回索引值，无则Error
index(sep)
11.判断是否由字母和数字组成
isalnum()
12.判断是否是全字母
isalpha()
13.判断是否是ASCII
isascii()
14.判断是否是数字
isdigit()
15.判断是否是keyword
isidentifier()
16.判断是否是全小写
islower()
17.判断是否是数字(unicode)
isnumeric()
18.判断是否可以打印
isprintable()
19.判断是否由空格组成
isspace()
20.判断是否首字母大写
istitle()
21.判断是否全大写
isupper()
22.以指定字符连接成新字符串
join()
23.字符串靠左对齐，填充至指定长度width
ljust(width)
24.所有字符小写
lower()
25.移除前导指定字符
lstrip()
26.使用转换对照表映射字符
maketrans()
27.找第一个出现的sep，分割为3元组
partition(sep)
28.将所有old字符替换为new
replace(old,new)
29.从右往左找指定字符，无则返回-1
rfind()
30.从右往左找指定字符，无则Error
rindex(sep)
31.字符串靠右对齐，填充至指定长度width
rjust(width)
32.从后往前找sep，分割为3元组
rpartition(sep)
33.从最右边开始以指定sep分割字符,n为分割次数
rsplit(sep,n)
34.移除末尾指定字符
rstrip()
35.以指定sep分割字符,n为分割次数
split(sep,n)
36.以keepends拆分字符串
splitlines([keepends])
37.判断第一个字符是否为指定字符
startswith()
38.移除首尾指定字符
strip()
39.大小写字符互相转换
swapcase()
40.使每个单词首字母大写，其他字母小写
title()
41.每个字符按给定转换表进行映射
translate()
42.所有大小写字符均准换为大写
upper()
43.左边填充0使其长度变为width
zfill(width)